import 'package:demo_unlock/screens/betting.dart';
import 'package:flutter/material.dart';

class BettingModeScreen extends StatelessWidget {
  const BettingModeScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BettingPage(
        version: '1.0.0+16 ios(updated patch 4) terminate true',
      ),
    );
  }
}
